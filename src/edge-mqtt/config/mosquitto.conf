# Persistence & logging
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log

# MQTT over TCP (for Picos and tools)
listener 1883
allow_anonymous true            # <-- change to false + add password_file after testing

# MQTT over WebSockets (for your browser client below)
listener 8080
protocol websockets
